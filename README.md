# watfaq.github.io

## Development

```bash
# Install dependencies
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pip install mkdocs

# Run development server
mkdocs serve
```